I2 = imread('DB0/db0_1.jpg');
%2 = im2double(I2);
HSV = rgb2hsv(I2);

H = HSV(:,:,1); H = H(:);
S = HSV(:,:,2); S = S(:);
V = HSV(:,:,3); V = V(:);
X = [H S V];


idx = kmeans(S, 2);
temp = reshape(idx, size(I2,1), size(I2, 2)),[];
%imshow(temp)
figure;
imshow(ind2rgb(reshape(idx, size(I2,1), size(I2, 2)), [0 0 1; 0 1 0]))

%clear